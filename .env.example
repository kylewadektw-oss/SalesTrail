# SalesTrail example environment variables
# Copy this file to `.env.local` and fill in real values before running locally.

# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
# Server-only (do NOT expose to the browser) - use only in server environment
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
VERCEL_AUTOMATION_BYPASS_SECRET=replace-with-your-32-char-secret

# Proxy base URL (for external RSS fetch proxy)
NEXT_PUBLIC_PROXY_BASE_URL=http://localhost:4000

# Optional: comma-separated allowed Craigslist city subdomains
NEXT_PUBLIC_ALLOWED_CITIES=hartford,newyork,boston,providence

# Authentication / NextAuth (if used)
NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-nextauth-secret

# Map provider (Mapbox, Google Maps, etc.)
NEXT_PUBLIC_MAPBOX_TOKEN=your-mapbox-token
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-key

# Google Maps
GOOGLE_MAPS_API_KEY=

# Optional: Twilio for SMS
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
SMS_ALLOWED_ORIGINS=

# Optional: Other proxies
NEXT_PUBLIC_PROXY_BASE_URL=
NEXT_PUBLIC_PROXY_BASE_URL_2=

# Database (if applicable)
# DATABASE_URL=postgres://user:pass@host:port/dbname

# Deployment / CI
# VERCEL_TOKEN=your-vercel-token
